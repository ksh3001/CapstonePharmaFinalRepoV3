# Manual path — supply options

Use this runbook when inference is unavailable.

1. Open the schema-valid pack produced by `python -m aegis run --workflow supply`.
2. Treat every option as draft. Do not reserve, allocate or ship.
3. Quality holds remain holds. Unqualified substitutes are described, not counted.
4. Reconcile outage-period drafts before any later AI-assisted run.
