# Change-class exceptions

An exception is required before a protected file may drift from `change-class-baseline.json`.

Format (one bullet per exception):

- `path` — reason — artefact 20 §4 record id

None recorded.
